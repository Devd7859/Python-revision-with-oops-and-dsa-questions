#Tuples are immutable data type in python
a = () #empty tuple
b = (1,) #tuple with only one element needs a comma
c = (1,7,2) #tuple with more than one element
print(type(b))
#methods of tuple
#count #index
#whenever will do operations on tuple it will create a new tuple it wont change the exisitng tuple 
